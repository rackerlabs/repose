<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.rackspace.papi</groupId>
        <artifactId>papi</artifactId>
        <version>2.7.1-SNAPSHOT</version>
    </parent>

    <groupId>com.rackspace.papi.</groupId>
    <artifactId>repose-spock-tests</artifactId>

    <name>Repose Tests - Spock Functional Tests</name>

    <packaging>jar</packaging>

    <properties>
        <groovy.version>2.1.3</groovy.version>
        <spock.version>0.7-groovy-2.0</spock.version>
        <gmaven.version>1.5</gmaven.version>
    </properties>


    <dependencies>
        <!-- We like Spock, test long and prosper -->
        <dependency>
            <groupId>org.codehaus.groovy</groupId>
            <artifactId>groovy-all</artifactId>
            <version>${groovy.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.spockframework</groupId>
            <artifactId>spock-core</artifactId>
            <version>${spock.version}</version>
            <scope>test</scope>
        </dependency>


        <dependency>
            <groupId>commons-io</groupId>
            <artifactId>commons-io</artifactId>
            <version>2.4</version>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.linkedin</groupId>
            <artifactId>org.linkedin.util-groovy</artifactId>
            <version>1.8.0</version>
        </dependency>
    </dependencies>

    <profiles>
        <profile>
            <id>spock-local</id>
            <activation>
                <activeByDefault>true</activeByDefault>
            </activation>
            <properties>

                <project.set.dir>${project.basedir}/../../project-set</project.set.dir>

                <repose.valve.dir>${project.set.dir}/core/valve/target</repose.valve.dir>
                <filter.dir>${project.set.dir}/components/filter-bundle/target</filter.dir>
                <filter.extensions.dir>${project.set.dir}/extensions/extensions-filter-bundle/target</filter.extensions.dir>

                <repose.valve.jar>repose-valve.jar</repose.valve.jar>
                <filter.ear>filter-bundle-${project.version}.ear</filter.ear>
                <filter.extensions.ear>extensions-filter-bundle-${project.version}.ear</filter.extensions.ear>

                <repose_port>8888</repose_port>
                <target_hostname>localhost</target_hostname>
                <target_port>9999</target_port>

                <repose.config.directory>${project.build.directory}/repose_home/configs</repose.config.directory>
                <repose.config.samples>${project.build.directory}/configs</repose.config.samples>
            </properties>
        </profile>
    </profiles>



    <build>
        <testResources>

            <testResource>
                <directory>${project.basedir}/src/test/resources/configs</directory>
                <filtering>true</filtering>
                <targetPath>${project.build.directory}/configs</targetPath>
            </testResource>

            <testResource>
                <directory>${filter.dir}</directory>
                <includes>
                    <include>${filter.ear}</include>
                </includes>
                <targetPath>${project.build.directory}/repose_home/artifacts</targetPath>
            </testResource>
            <testResource>
                <directory>${filter.extensions.dir}</directory>
                <includes>
                    <include>${filter.extensions.ear}</include>
                </includes>
                <targetPath>${project.build.directory}/repose_home/artifacts</targetPath>
            </testResource>
            <testResource>
                <directory>${repose.valve.dir}</directory>
                <includes>
                    <include>${repose.valve.jar}</include>
                </includes>
                <targetPath>${project.build.directory}/repose_home</targetPath>
            </testResource>

        </testResources>


        <plugins>
            <plugin>
                <groupId>org.codehaus.gmaven</groupId>
                <artifactId>gmaven-plugin</artifactId>
                <version>${gmaven.version}</version>
                <configuration>
                    <source/>
                    <providerSelection>1.8</providerSelection>
                </configuration>
                <dependencies>
                    <dependency>
                        <groupId>org.codehaus.gmaven.runtime</groupId>
                        <artifactId>gmaven-runtime-2.0</artifactId>
                        <version>${gmaven.version}</version>
                        <exclusions>
                            <exclusion>
                                <groupId>org.codehaus.groovy</groupId>
                                <artifactId>groovy-all</artifactId>
                            </exclusion>
                        </exclusions>
                    </dependency>
                    <dependency>
                        <groupId>org.codehaus.groovy</groupId>
                        <artifactId>groovy-all</artifactId>
                        <version>${groovy.version}</version>
                    </dependency>
                </dependencies>
                <executions>
                    <execution>
                        <id>buildGroovies</id>
                        <goals>
                            <goal>generateStubs</goal>
                            <goal>compile</goal>
                            <goal>generateTestStubs</goal>
                            <goal>testCompile</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>2.9</version>
                <configuration>
                    <systemProperties>
                        <property>
                            <name>repose.config.directory</name>
                            <value>${repose.config.directory}</value>
                        </property>
                        <property>
                            <name>repose.config.samples</name>
                            <value>${repose.config.samples}</value>
                        </property>
                    </systemProperties>
                </configuration>
            </plugin>

        </plugins>
    </build>
</project>
