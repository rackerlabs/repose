{
  dir_prefix: "filters/splitheader",
  repose: {
    config: {
      system_model: {
        filters: [
          { name: "split-header" }
        ]
      }
    },
    config_files: {
      files: [
        "log4j2.xml",
        "{{ dir_prefix }}/split-header.cfg.xml"
      ]
    }
  },
  mock: {
    backends: [
      "main_backend.js"
    ]
  },
  gatling: {
    test: {
      name: "SplitHeaderFilterSimulation",
      package: "filters.splitheader"
    }
  }
}
