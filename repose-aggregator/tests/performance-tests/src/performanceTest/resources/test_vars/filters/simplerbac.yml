{
  dir_prefix: "filters/simplerbac",
  repose: {
    config: {
      system_model: {
        filters: [
          { name: "simple-rbac" }
        ]
      }
    },
    config_files: {
      files: [
        "{{ dir_prefix }}/simple-rbac.cfg.xml"
      ]
    }
  },
  gatling: {
    test: {
      package: "filters.simplerbac",
      name: "SimpleRbacFilterSimulation"
    }
  }
}
