{
  dir_prefix: "filters/translation",
  repose: {
    config: {
      system_model: {
        filters: [
          { name: "translation" }
        ]
      }
    },
    config_files: {
      files: [
        "{{ dir_prefix }}/path-to-query.xsl",
        "{{ dir_prefix }}/translation.cfg.xml"
      ],
      templates: [
        "container.cfg.xml",
        "system-model.cfg.xml"
      ]
    }
  },
  gatling: {
    test: {
      name: "TranslationFilterSimulation",
      package: "filters.translation"
    }
  }
}
