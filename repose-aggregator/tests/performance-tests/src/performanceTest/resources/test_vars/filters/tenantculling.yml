{
  dir_prefix: "filters/tenantculling",
  repose: {
    config: {
      system_model: {
        filters: [
          { name: "tenant-culling", uri_regex: '.*/test' }
        ]
      }
    },
    config_files: {
      files: [
        "log4j2.xml"
      ],
      templates: [
        "container.cfg.xml",
        "system-model.cfg.xml"
      ]
    }
  },
  mock: {
    backends: [
      "main_backend.js"
    ]
  },
  gatling: {
    test: {
      name: "TenantCullingFilterSimulation",
      package: "filters.tenantculling"
    }
  }
}
