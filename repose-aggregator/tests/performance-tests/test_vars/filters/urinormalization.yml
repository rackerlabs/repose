{
  dir_prefix: "filters/urinormalization",
  repose: {
    config: {
      system_model: {
        filters: [
          { name: "uri-normalization" }
        ]
      }
    },
    config_files: {
      files: [
        "log4j2.xml",
        "{{ dir_prefix }}/uri-normalization.cfg.xml"
      ],
      templates: [
        "container.cfg.xml",
        "system-model.cfg.xml"
      ]
    }
  },
  mock: {
    backends: [
      "main_backend.js"
    ]
  },
  gatling: {
    test: {
      name: "UriNormalizationFilterSimulation",
      package: "filters.urinormalization"
    }
  }
}
