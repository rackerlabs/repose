{
  dir_prefix: "filters/apivalidator",
  repose: {
    config: {
      system_model: {
        filters: [
          { name: "api-validator" }
        ]
      }
    },
    config_files: {
      files: [
        "{{ dir_prefix }}/validator.cfg.xml",
        "{{ dir_prefix }}/validator.wadl"
      ]
    }
  },
  gatling: {
    test: {
      package: "filters.apivalidator",
      name: "ApiValidatorFilterSimulation"
    }
  }
}
