{
  dir_prefix: "services/uriredaction",
  repose: {
    config: {
      system_model: {
        services: [
          # todo: when it becomes a service, add "uri-redaction"
        ]
      }
    },
    config_files: {
      files: [
        "{{ dir_prefix }}/log4j2.xml",
        "{{ dir_prefix }}/uri-redaction.cfg.xml"
      ]
    }
  },
  mock: {
    backends: [
      "main_backend.js"
    ]
  },
  gatling: {
    test: {
      name: "UriRedactionServiceSimulation",
      package: "services.uriredaction"
    }
  }
}
