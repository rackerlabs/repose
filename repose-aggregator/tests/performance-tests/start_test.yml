- hosts: localhost
  connection: local
  gather_facts: True
  vars_files:
    - test_vars/{{ perf_test }}.yml
  roles:
    - cloud_resource_create

- hosts: performance
  gather_facts: True
  vars_files:
      - test_vars/{{ perf_test }}.yml
  roles:
    - rossmcdonald.telegraf
    - repose

- hosts: test_agent
  gather_facts: True
  vars_files:
      - test_vars/{{ perf_test }}.yml
  roles:
    - gatling
