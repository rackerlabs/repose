package ThisPackageNeedsABlankLine
import scala.util.Random;
class s(xi: Int)  {
  var x: Int = xi
  def getX ={
    for (5 <- Range){
      while(true) {
        if  (true) {

        } else
        if (true) {

        } else{

        }
      }
      Random r = new Random();
      try{

      } catch {
        case e: Exception => println("exception caught: " + e);
      } finally  {

      }

      do  {

      } while (true)

      if (true){

      } else  {

      }
      while (true)     {

      }
      if (true)
      {

      }
      import java.util.ArrayList;
      ArrayList e;


      x match {
        case A => // compare x to A, because of the uppercase
        case b => // assign x to b
        case `b` => // compare x to b, because of the backtick
      }
    }
  }



}
//this breaks things


